# Configures the documentation build on Read-the-Docs.

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3"

sphinx:
   configuration: docs/conf.py

python:
   install:
   - method: pip
     path: .
     extra_requirements:
     - dev
